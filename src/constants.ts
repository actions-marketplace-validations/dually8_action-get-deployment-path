/* eslint-disable no-unused-vars */
// Do this because these are used elsewhere but eslint is dumb :)
export enum Inputs {
  EnvironmentName = 'environment-name',
  DevPath = 'dev-path',
  StagingPath = 'staging-path',
  ProdPath = 'prod-path'
}
export enum Outputs {
  DeploymentPath = 'deployment-path'
}
